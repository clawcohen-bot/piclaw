{
  "name": "telegram-pi-agent",
  "projectType": "application",
  "sourceRoot": "apps/telegram-pi-agent/src",
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bash -c 'while true; do tsx --env-file=.env apps/telegram-pi-agent/src/main.ts; code=$?; if [ \"$code\" != \"75\" ]; then exit \"$code\"; fi; echo Reloading telegram-pi-agent...; done'"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p apps/telegram-pi-agent/tsconfig.json --noEmit"
      }
    }
  }
}
